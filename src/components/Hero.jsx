import { motion } from 'framer-motion';
import { styles } from '../styles';
import { CoffeeBreakCanvas } from './canvas';

const Hero = () => {
  return <section className='relative w-full h-screen mx-auto'>sdgf</section>;
};

export default Hero;
